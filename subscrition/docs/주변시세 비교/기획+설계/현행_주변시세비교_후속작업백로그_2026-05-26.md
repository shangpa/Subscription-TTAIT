# [현행] 주변시세 비교 후속 작업 백로그 2026-05-26

<!-- markdownlint-disable MD013 -->

## 1. 문서 목적

이 문서는 주변시세 비교 API를 팀원에게 넘긴 뒤에도 잊지 말아야 할 후속 작업을 정리한다.
현재 완료 기준은 `RTMS raw 수집`, `MarketPriceSnapshot 수동 집계`, `공고 unit 기준 주변시세 비교 API`, `프론트/팀원 AI 인수인계 문서`까지다.
아래 항목은 현재 PR의 blocker가 아니라 다음 sprint 또는 운영 안정화 작업으로 관리한다.

## 2. 현재 완료된 범위

| 항목 | 상태 | 메모 |
| - | - | - |
| 최신 main smoke test | 완료 | `bash ./gradlew test`, `bootRun` 확인 |
| RTMS 수동 수집 API | 완료 | `POST /api/admin/market/rtms/collect` |
| `MarketPriceSnapshot` 수동 집계 API | 완료 | `POST /api/admin/market/snapshots/aggregate` |
| 주변시세 비교 public API | 완료 | `GET /api/announcements/{announcementId}/units/{unitId}/market-comparison` |
| 프론트/팀원 AI 인수인계 문서 | 완료 | `현행_주변시세비교_API인수인계_2026-05-26.md` |
| 실데이터 E2E 검증 | 진행 예정 | LH 수집부터 비교 API까지 실제 데이터로 확인 필요 |

## 3. PR 전 실데이터 E2E 검증 체크리스트

### 3.1 준비

```bash
cd ~/dev/Subscription-TTAIT-market-comparison-api/subscrition

docker compose -f ~/dev/Subscription-TTAIT/subscrition/docker-compose.local.yml up -d
SPRING_CONFIG_IMPORT="optional:file:/home/user/dev/Subscription-TTAIT/subscrition/.env[.properties]" bash ./gradlew bootRun
```

Swagger UI:

```text
http://localhost:8080/swagger-ui/index.html
```

### 3.2 확인 순서

1. LH 수집 API를 실제로 호출한다.
2. 수집 결과로 `announcement`, `announcement_unit`이 생성되는지 확인한다.
3. 대상 unit에 `exclusive_area_value`, `deposit_amount`, `monthly_rent_amount`가 있는지 확인한다.
4. 대상 unit에 `lawd_cd`가 채워져 있는지 확인한다.
5. `lawd_cd`가 없으면 주소 정규화/법정동 매핑 후속 작업이 필요하다.
6. RTMS 수동 수집 API로 같은 `lawdCd`, `dealYm`의 raw 거래를 수집한다.
7. `market_transaction_raw`에 row가 저장되는지 확인한다.
8. snapshot 집계 API를 호출한다.
9. `market_price_snapshot`에 snapshot이 저장되고 `status`가 `OK` 또는 `INSUFFICIENT_DATA`인지 확인한다.
10. 주변시세 비교 public API를 실제 `announcementId`, `unitId`로 호출한다.
11. `COMPARABLE`, `SNAPSHOT_NOT_FOUND`, `UNIT_LAWD_CD_MISSING`, `UNIT_AREA_MISSING`, `INSUFFICIENT_DATA` 중 어떤 status가 나오는지 기록한다.

### 3.3 DB 확인 쿼리 예시

```sql
select id, notice_name, region_level1, region_level2, full_address
from announcement
order by id desc
limit 10;

select id, announcement_id, full_address, lawd_cd, exclusive_area_value, deposit_amount, monthly_rent_amount
from announcement_unit
order by id desc
limit 20;

select id, source_type, lawd_cd, deal_ym, exclusive_area, deposit_amount, monthly_rent_amount, trade_amount
from market_transaction_raw
order by id desc
limit 20;

select id, source_type, lawd_cd, deal_ym_from, deal_ym_to, area_min, area_max, sample_count, status
from market_price_snapshot
order by id desc
limit 20;
```

## 4. 후속 작업 우선순위

| 우선순위 | 작업 | 이유 |
| - | - | - |
| 1 | 실데이터 E2E 검증 결과 문서화 | 팀원 인수인계 전 실제 흐름 확인 필요 |
| 2 | `lawdCd` 누락 unit 처리 | 비교 API는 unit의 `lawdCd`가 있어야 snapshot을 찾을 수 있음 |
| 3 | RTMS `resultCode=03 No Data` 정책 확정 | 현재 `resultCode != 00`은 실패로 처리하므로 빈 데이터 정책 정리 필요 |
| 4 | RTMS 전체 pagination 수집 | 현재는 단일 page 수동 수집만 완료 |
| 5 | snapshot 자동 재집계 batch/scheduler | 운영에서 매번 수동 집계하기 어렵기 때문 |
| 6 | 비교 기준 고도화 | 면적 허용 오차, 최근 N개월 default, sourceType 자동 매핑 필요 |
| 7 | 프론트 상태 표시 정책 확정 | status별 UI 문구와 fallback을 일관되게 맞추기 위함 |
| 8 | 운영 로그/재시도 정책 | 외부 API 실패와 수집 실패를 추적하기 위함 |

## 5. 팀원에게 넘길 때 같이 전달할 내용

- public 비교 API는 이미 생성된 snapshot을 조회하는 API다.
- 프론트가 직접 RTMS를 호출하지 않는다.
- `SNAPSHOT_NOT_FOUND`는 프론트 오류가 아니라 해당 조건의 snapshot이 아직 없다는 뜻이다.
- `UNIT_LAWD_CD_MISSING`은 주소 정규화/법정동 코드 매핑이 선행되지 않았다는 뜻이다.
- `INSUFFICIENT_DATA`는 snapshot은 있으나 sample 수가 부족하다는 뜻이다.
- 실제 UI에서는 `COMPARABLE`만 수치 비교를 강조하고, 나머지는 안내 문구로 처리하는 편이 안전하다.

## 6. 이후 별도 PR 후보

1. `lawdCd` 매핑 데이터 보강 및 주소 정규화 관리자 API.
2. RTMS 전체 pagination 수집 API 또는 batch.
3. snapshot batch/scheduler와 재집계 정책.
4. 주변시세 비교 default parameter 정책.
5. 프론트 연동용 mock response와 Apidog/Postman collection.
